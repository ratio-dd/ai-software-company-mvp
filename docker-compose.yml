services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      PROVIDER_MODE_DEFAULT: mock
      DATA_DIR: /app/data
      LLM_BASE_URL: ${LLM_BASE_URL:-}
      LLM_MODEL: ${LLM_MODEL:-}
      LLM_API_KEY: ${LLM_API_KEY:-}
    volumes:
      - ./data:/app/data
